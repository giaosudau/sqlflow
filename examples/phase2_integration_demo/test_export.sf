
-- Test 2: Export data to S3 in Parquet format (more efficient)
EXPORT SELECT 
    order_id,
    customer_id,
    order_date,
    total_amount,
    status,
    created_at,
    updated_at
FROM staging_orders_s3 
TO 's3://sqlflow-demo/exports/orders.parquet' 
TYPE PARQUET OPTIONS {};

